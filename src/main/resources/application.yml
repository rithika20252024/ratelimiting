server:
  port: 8080

spring:
  application:
    name: rateshield
  datasource:
    url: jdbc:h2:mem:rateshield;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true

rateshield:
  default-algorithm: TOKEN_BUCKET
  default-max-requests: 10
  default-window-seconds: 60
